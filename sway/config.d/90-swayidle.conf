# Idle configuration
set $lock ~/.config/sway/scripts/lock.sh
set $suspend ~/.config/sway/scripts/suspend.sh

exec swayidle -w \
    timeout 120 $lock \
    timeout 300 $suspend \
    before-sleep $lock
